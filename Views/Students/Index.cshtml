@model List<NUTRIBITE.Models.Food>

@{
    ViewData["Title"] = "NutriBite — Students";
    Layout = "~/Views/Shared/_PublicLayout.cshtml";
}

<link rel="stylesheet" href="~/css/students.css" />

<section class="students-hero container my-4" role="region" aria-label="Students hero">
    <div class="hero-card">
        <div class="hero-left">
            <h1>Fuel Your Hustle, Student!</h1>
            <p class="lead">Affordable, high-protein, and brain-boosting meals for your busy life.</p>
            <a class="btn explore-combos" href="#studentMeals">Explore Student Specials</a>
        </div>
        <div class="hero-right" aria-hidden="true">
            <img src="~/images/categories/student.jpeg" alt="Student meals" loading="lazy" />
        </div>
    </div>
</section>

<!-- Sticky filter bar -->
<section class="filter-bar-wrap" aria-label="Student filters">
    <div class="filter-bar container" role="region" aria-label="Filters" id="studentsFilterBar">
        <div class="filter-item">
            <label for="priceRange">Budget: <span id="priceValue">₹100</span></label>
            <input id="priceRange" type="range" min="50" max="150" value="100" />
        </div>

        <div class="filter-item toggle-item">
            <label><input id="highProtein" type="checkbox" /> High Protein</label>
        </div>

        <div class="filter-item toggle-item">
            <label><input id="under500" type="checkbox" /> Under 500 kcal</label>
        </div>

        <div class="filter-item toggle-item">
            <label><input id="vegOnly" type="checkbox" /> Veg Only</label>
        </div>

        <div class="filter-item">
            <button id="resetFilters" class="btn reset-btn">Reset</button>
        </div>
    </div>
</section>

@section Styles {
    <style>
        .verification-badge {
            display: inline-flex;
            align-items: center;
            gap: 5px;
            background: #e6f9f0;
            color: #2d6a4f;
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            margin-bottom: 10px;
            border: 1px solid #c2f0d5;
        }

        .nutrition-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 8px;
            margin: 12px 0;
            padding: 10px;
            background: #f8f9fa;
            border-radius: 8px;
            font-size: 0.8rem;
        }

        .nutrition-item {
            display: flex;
            justify-content: space-between;
            color: #666;
        }

        .nutrition-item strong {
            color: #333;
        }

        .nutritionist-info {
            font-size: 0.7rem;
            color: #888;
            font-style: italic;
            margin-top: 5px;
        }
    </style>
}

<!-- All Student Meals Combined -->
<section id="studentMeals" class="container my-5" aria-labelledby="studentMealsHeading">
    <h2 id="studentMealsHeading">Student Specials Menu</h2>

    <div class="combos-grid" role="list">
        @if (Model != null && Model.Any())
        {
            foreach (var food in Model)
            {
                <article class="combo-card" role="listitem"
                         data-calories="@food.Calories"
                         data-price="@food.Price"
                         data-veg="true">
                    
                    @if (food.IsVerified)
                    {
                        <div class="verification-badge">
                            <i class="bi bi-patch-check-fill"></i> Verified by Nutritionist
                        </div>
                    }

                    <div class="combo-badge">@food.Calories kcal</div>
                    <!-- LEFT SIDE (TEXT CONTENT) -->
                    <div class="combo-content">
                        <h3 class="combo-title">@food.Name</h3>

                        <div class="nutrition-grid">
                            <div class="nutrition-item"><span>Protein:</span> <strong>@(food.Protein ?? 0)g</strong></div>
                            <div class="nutrition-item"><span>Carbs:</span> <strong>@(food.Carbs ?? 0)g</strong></div>
                            <div class="nutrition-item"><span>Fat:</span> <strong>@(food.Fat ?? 0)g</strong></div>
                            <div class="nutrition-item"><span>Prep:</span> <strong>@food.PreparationTime</strong></div>
                        </div>

                        <div class="combo-price">₹<strong>@food.Price</strong></div>
                        
                        @if (food.Nutritionist != null)
                        {
                            <div class="nutritionist-info">
                                Certified Expert: @food.Nutritionist.Name
                            </div>
                        }

                        <button class="btn add-cart" data-id="@food.Id">Add to Cart</button>
                    </div>

                    <!-- RIGHT SIDE (IMAGE) -->
                    <div class="combo-image">
                        <img src="@(string.IsNullOrEmpty(food.ImagePath) ? "/images/menu items/default.png" : food.ImagePath)"
                             alt="@food.Name"
                             loading="lazy" />
                    </div>
                </article>
            }
        }
        else
        {
            <div class="alert alert-info w-100">No student special meals found in the database. Please run SeedData.</div>
        }
    </div>
</section>

@section Scripts {
    <script src="~/js/students.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const priceRange = document.getElementById("priceRange");
            const priceValue = document.getElementById("priceValue");
            const highProtein = document.getElementById("highProtein");
            const under500 = document.getElementById("under500");
            const vegOnly = document.getElementById("vegOnly");
            const resetBtn = document.getElementById("resetFilters");
            const comboCards = document.querySelectorAll(".combo-card");

            function applyFilters() {
                const maxPrice = parseInt(priceRange.value);
                priceValue.textContent = "₹" + maxPrice;

                comboCards.forEach(card => {
                    const price = parseInt(card.dataset.price);
                    const calories = parseInt(card.dataset.calories);
                    const isVeg = card.dataset.veg === "true";

                    let visible = true;

                    if (price > maxPrice) visible = false;
                    if (highProtein.checked && calories < 600) visible = false;
                    if (under500.checked && calories >= 500) visible = false;
                    if (vegOnly.checked && !isVeg) visible = false;

                    card.style.display = visible ? "flex" : "none";
                });
            }

            priceRange.addEventListener("input", applyFilters);
            highProtein.addEventListener("change", applyFilters);
            under500.addEventListener("change", applyFilters);
            vegOnly.addEventListener("change", applyFilters);

            resetBtn.addEventListener("click", () => {
                priceRange.value = 100;
                highProtein.checked = false;
                under500.checked = false;
                vegOnly.checked = false;
                applyFilters();
            });
        });
    </script>
}
